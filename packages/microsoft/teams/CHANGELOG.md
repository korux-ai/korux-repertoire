# Changelog

## 1.0.0

- Rename flat `microsoft-teams` → `microsoft/teams` (alias `microsoft-teams`)
- Real Microsoft Graph identity verify (bearer `/me` or app-only `/organization`)
- Mock only when `KORUX_CAPABILITY_HTTP_MOCK=1`
