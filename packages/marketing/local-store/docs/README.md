# marketing/local-store

Catalog **skill** (no `runtime/`). Local / multi-location campaign planning; does not publish.

## Pipeline

**local-store** (or `campaign-brief`) → `channel-mix` → `content-calendar` / `ab-copy` → social/CMS/email connectors.

No Vault binding.
