# marketing/content-calendar

Catalog **skill** (no `runtime/`). Produces an editorial/content calendar; does not publish.

## Pipeline

`campaign-brief` → `channel-mix` → **content-calendar** → `ab-copy` / compose → connectors.

No Vault binding.
