# Changelog — fedwatch/probabilities

## 1.0.0

- Initial connector: configurable Fed odds JSON proxy + optional Yahoo ZQ=F under-anchored backup.
- Language-neutral structured output (`role`, `confidence`, `as_of`, `meetings`).
