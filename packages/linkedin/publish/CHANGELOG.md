# Changelog

## 1.0.0

- First-party LinkedIn Company Page post: text, optional one JPEG/PNG
- Package `runtime.invoke` via Posts API + Images API (real HTTPS; mock only when `KORUX_CAPABILITY_HTTP_MOCK=1`)
- Owner governor: blocked keywords / URL hosts / max chars; optional max mentions / require image
