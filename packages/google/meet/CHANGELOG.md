# Changelog

## 1.0.0

- Rename flat `google-meet` → `google/meet` (alias `google-meet`)
- Real Google OAuth userinfo identity verify (refresh token or access_token)
- Mock only when `KORUX_CAPABILITY_HTTP_MOCK=1`
