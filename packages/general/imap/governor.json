{
  "spec_version": "korux_governor_v1",
  "capability_id": "general/imap",
  "capability_version": "1.0.0",
  "defaults": {
    "intercept_on_invoke": false,
    "editable_fields": [],
    "non_editable_fields": []
  },
  "rules": [
    {
      "id": "external_read_auto",
      "when": {
        "reads_external": true
      },
      "action": "pass",
      "gate": "auto",
      "message": {
        "en": "IMAP poll is External read; default gate auto",
        "zh_CN": "IMAP 轮询为 External 读取；默认 gate 为 auto",
        "zh_HK": "IMAP 輪詢為 External 讀取；預設 gate 為 auto"
      }
    }
  ],
  "implementation": "declarative",
  "notes": "Evaluated by Korux platform declarative engine; optional runtime/governor.py overrides."
}
